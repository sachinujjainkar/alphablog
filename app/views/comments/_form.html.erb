
<%= form_with(model: comment, local: true, :html => {class: "form-horizontal myform", role: "form"}) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
     <div class="control-label col-sm-2">
          <%= form.label :description, "Comment" %>
      </div>
      <div class="col-sm-8">
        <%= form.text_field :description, class: "form-control", placeholder: "Add Comment", autofocus: true %>
      </div>
    <br>
   </div>
    <%= form.hidden_field :User_id , value: current_user.id %>
    <%= form.hidden_field :article_id %>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-2">
        <%= form.submit "Add", class: "btn btn-primary btn-lg"%>
      </div>  
    </div> 
<% end %>


     

    